@* @model IEnumerable<IRCTCClone.Models.Train>

<h2>My Bookings</h2>

@if (!Model.Any())
{
    <p>No bookings found</p>
}
else
{
    <table class="table table-bordered table-hover">
        <thead class="table-light">
            <tr>
                <th>PNR</th>
                <th>Train</th>
                <th>Class</th>
                <th>Journey Date</th>
                <th>Amount</th>
                <th>Status</th>
                <th>Details</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var b in Model)
            {
                <tr>
                    <td>@b.PNR</td>
                    <td>@b.Number - @b.Name</td>
                    <td>@b.Classes</td>
                    <td>@b.JourneyDate?.ToString("dd-MM-yyyy")</td>
                    <td>₹@b.Amount</td>
                    <td>@b.Status</td>
                    <td>
                        <a href="/Booking/Details/@b.Id" class="btn btn-info btn-sm">View</a>
                    </td>
                    <td>
                        <form asp-action="Cancel" asp-controller="Booking" method="post" style="display:inline;">
                            <input type="hidden" name="id" value="@b.Id" />
                        </form>
                        @if (User.Identity.IsAuthenticated && b.Status?.ToUpper() == "CONFIRMED")
                        {
                            <a asp-action="DownloadTicket" asp-route-id="@b.Id"
                               class="btn btn-outline-primary btn-sm ms-1">
                                Download E-Ticket (PDF)
                            </a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
 *@